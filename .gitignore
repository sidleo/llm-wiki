node_modules/
.DS_Store
pnpm-lock.yaml
*.tgz
package-lock.json

# 用户私有知识 bundle（真实迁移目标，不进仓库）
~/.agents/wiki/
.agents/

# 根 AGENTS.md 不上 GitHub（本地开发规范，发布仓库不含）
/AGENTS.md
/CLAUDE.md
/CODEBUDDY.md

# 知识库 bundle 产物（代码仓库根目录绝不应出现；防止 git add -A 误收私有内容）
/log.md
/index.md
